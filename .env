# Variables in this file are only used by the server application and not the client application.

# MongoDB connection URI for the server application.
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority
MONGODB_URI=mongodb://127.0.0.1:27017/InventoryWarehouse